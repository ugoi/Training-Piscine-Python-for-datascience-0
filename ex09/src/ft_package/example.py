def count_in_list(list: list, string: str) -> int:
    """
    Count occurrences of a term in a list.
    Args:
    - lst (List): The list to search in.
    - term: The term to search for.
    Returns:
    int: Number of occurrences of the term in the list.
    """
    return list.count(string)
